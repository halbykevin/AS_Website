requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
pandas>=2.0
openpyxl>=3.1
PyYAML>=6.0
tqdm>=4.66

# Optional: only needed for JavaScript-rendered sites (--render).
# After installing, run once:  playwright install chromium
playwright>=1.40
