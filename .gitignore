node_modules
dist
dist-ssr
*.local
.DS_Store
.env
.vscode/*
!.vscode/extensions.json

# Web scraper run output (per-job folders created at runtime)
server/scrapes/

# Deploy: local SSH target + the VPS-side state (schema fingerprint, db dumps)
deploy.env
.deploy-state/

# Dev server logs
dev-server.log
**/dev-server.log
