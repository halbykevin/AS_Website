node_modules
dist
dist-ssr
*.local
.DS_Store
.env
.vscode/*
!.vscode/extensions.json

# Web scraper run output (per-job folders created at runtime)
server/scrapes/

# Set-aside template git history (local safety backup, not for the repo)
EcomWebsite/.git_template_backup/

# Dev server logs
dev-server.log
**/dev-server.log
